# Web App

This crate contains the web app entrypoint, favicon, web CSS, and optional fullstack server feature wiring.

Run it from the workspace root:

```sh
dx serve --package web --web
```

The workspace root also defaults to this package, so `dx serve` is enough for normal web development.
